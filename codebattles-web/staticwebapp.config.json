{
    "routes": [
      {
        "route": "/api/:path*",
        "rewrite": "/:path*",
        "destination": {
          "host": "codebattlesapi.azurewebsites.net",
          "changeOrigin": true
        }
      }
    ]
  }